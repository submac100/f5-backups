---
# Start with ONE device to prove the flow end to end.
# To cover HA pairs, just add both members under `hosts:` — the play
# backs up every host in the `bigip` group independently.
all:
  children:
    bigip:
      hosts:
        bigip-lab-01:
          ansible_host: 192.0.2.10        # <-- management IP of your test BIG-IP

        # --- example HA pair (uncomment + edit when ready to scale) ---
        # bigip-syd-01a:
        #   ansible_host: 10.10.10.11
        # bigip-syd-01b:
        #   ansible_host: 10.10.10.12
